const [passengerAllocations, setPassengerAllocations] = useState<Record<string, { room: string, vehicle: string, seat: string }>>({});  const [allocFleet, setAllocFleet] = useState<any[]>([    { id: "tempo-1", vehicleType: "17 Seater Tempo", capacity: 17 }

  ]);~
z    { id: "tempo-1", name: "Tempo 1", vehicleType: "13 Seater Tempo", capacity: 13, cost: 45000, vendor: "ABC Travels" },~
z    { id: "tempo-2", name: "Tempo 2", vehicleType: "17 Seater Tempo", capacity: 17, cost: 58000, vendor: "XYZ Travels" },v
r    { id: "car-1", name: "Car 1", vehicleType: "6 Seater Car", capacity: 6, cost: 22000, vendor: "Self-driven" },

  ]);
O
K  const [newVehicleType, setNewVehicleType] = useState("17 Seater Tempo");@
<  const [newVehicleName, setNewVehicleName] = useState("");@
<  const [newVehicleCost, setNewVehicleCost] = useState("");D
@  const [newVehicleVendor, setNewVehicleVendor] = useState("");
9
5  const handleAddVehicle = (e: React.FormEvent) => {
    e.preventDefault();a
]    const cap = newVehicleType.includes("17") ? 17 : newVehicleType.includes("13") ? 13 : 6;
    const newV = {'
#      id: `vehicle-${Date.now()}`,D
@      name: newVehicleName || `Tempo ${allocFleet.length + 1}`,'
#      vehicleType: newVehicleType,
      capacity: cap,3
/      cost: parseInt(newVehicleCost) || 35000,7
3      vendor: newVehicleVendor || "General Vendor"
    };.
*    setAllocFleet([...allocFleet, newV]);
    setNewVehicleName("");
    setNewVehicleCost("");!
    setNewVehicleVendor("");M
I    toast.success(`Added ${newV.name} (${newV.vehicleType}) to fleet!`);	
  };
4
0  const handleDeleteVehicle = (id: string) => {<
8    setAllocFleet(allocFleet.filter(v => v.id !== id));2
.    toast.info("Removed vehicle from fleet");	
  };
*
&  const handleCopyTempoList = () => {;
7    let txt = "*Tempo List (for WhatsApp Group)*\n\n";5
1    const groups: Record<string, string[]> = {};2
.    computedVehicleAllocations.forEach(v => {4
0      const vName = v.vehicleType || "Tempo 1";2
.      if (!groups[vName]) groups[vName] = [];.
*      groups[vName].push(v.travelerName);
    });
=
9    Object.entries(groups).forEach(([vName, names]) => {V
R      txt += `🚌 *${vName}* — ${names.join(", ")} [${names.length} names]\n`;
    });
,
(    navigator.clipboard.writeText(txt);C
?    toast.success("WhatsApp Tempo List copied to clipboard!");	
  };
)
%  const handleCopyRoomList = () => {:
6    let txt = "*Room List (for WhatsApp Group)*\n\n";P
L    const groups: Record<string, { gender: string, names: string[] }> = {};/
+    computedRoomAllocations.forEach(r => {b
^      if (!groups[r.roomNumber]) groups[r.roomNumber] = { gender: r.genderGroup, names: [] };;
7      groups[r.roomNumber].names.push(r.travelerName);
    });
=
9    Object.entries(groups).forEach(([roomNo, data]) => {
      txt += `🏢 *${roomNo}* — ${data.names.join(", ")} (${data.gender === "BOYS" ? "Boys" : data.gender === "GIRLS" ? "Girls" : "Couples"})\n`;
    });
,
(    navigator.clipboard.writeText(txt);B
>    toast.success("WhatsApp Room List copied to clipboard!");	
  };
  const computedRoomAllocations = useMemo(() => {    const list: any[] = [];    Object.entries(passengerAllocations).forEach(([name, alloc]) => {      if (alloc.room && alloc.room !== "Unassigned" && alloc.room !== "—") {        const gender = name.includes("Das") ? "COUPLE" : (name === "Sneha Reddy" || name === "Pooja Hegde") ? "GIRLS" : "BOYS";        list.push({          roomNumber: alloc.room,          travelerName: name,          genderGroup: gender,          roomType: "Double"        });      }    });    return list;  }, [passengerAllocations]);  const computedVehicleAllocations = useMemo(() => {    const list: any[] = [];    Object.entries(passengerAllocations).forEach(([name, alloc]) => {      if (alloc.vehicle && alloc.vehicle !== "Unassigned" && alloc.vehicle !== "—") {        list.push({          fleetId: "tempo-1",          vehicleType: alloc.vehicle,          seatNumber: alloc.seat,          travelerName: name        });      }    });    return list;  }, [passengerAllocations]);  const [sharingPref, setSharingPref] = useState<string>("3");  const [sameGenderEnforced, setSameGenderEnforced] = useState(true);  const [prioritizeCouples, setPrioritizeCouples] = useState(true);  const [fallbackToQuad, setFallbackToQuad] = useState(true);  const handleTriggerAutoAllocate = () => {    const newAllocs: Record<string, any> = {};    let roomNum = 1;    let seatNum = 1;    // Filter active travelers    const activeTravelers = allPassengers.filter(p => p.notes !== "Cancelled");    // Separate couples (same last name or co-travelers booked together)    const couples = activeTravelers.filter(p => p.name.includes("Das") || p.name.includes("Sharma") && p.name.includes("Amit"));    const sameGenderGirls = activeTravelers.filter(p => p.name === "Sneha Reddy" || p.name === "Pooja Hegde");    const males = activeTravelers.filter(p => p.gender === "Male" && !couples.find(c => c.name === p.name));    const females = activeTravelers.filter(p => p.gender === "Female" && !couples.find(c => c.name === p.name) && !sameGenderGirls.find(g => g.name === p.name));    // 1. Couples for 2-sharing    if (prioritizeCouples && couples.length > 0) {      couples.forEach((p, idx) => {        newAllocs[p.name] = {          room: `Group No. ${roomNum}`,          vehicle: "17 Seater Tempo",          seat: String(seatNum++)        };        if (idx % 2 === 1) roomNum++;      });    }    // 2. Same gender female pairs in double    if (sameGenderGirls.length > 0) {      sameGenderGirls.forEach((p, idx) => {        newAllocs[p.name] = {          room: `Group No. ${roomNum}`,          vehicle: "17 Seater Tempo",          seat: String(seatNum++)        };        if (idx % 2 === 1) roomNum++;      });    }    // 3. Males sharing (default 3 sharing)    const targetSize = parseInt(sharingPref) || 3;    let maleCount = 0;    males.forEach((p) => {      newAllocs[p.name] = {        room: `Group No. ${roomNum}`,        vehicle: "17 Seater Tempo",        seat: String(seatNum++)      };      maleCount++;      if (maleCount >= targetSize) {        maleCount = 0;        roomNum++;      }    });    if (maleCount > 0) {      if (fallbackToQuad && maleCount < targetSize) {        // Fallback: merge into quad with the same group        toast.info("Fallback rule applied: leftovers merged into quad sharing");      } else {        roomNum++;      }    }    // 4. Females sharing (default 3 sharing)    let femaleCount = 0;    females.forEach((p) => {      newAllocs[p.name] = {        room: `Group No. ${roomNum}`,        vehicle: "17 Seater Tempo",        seat: String(seatNum++)      };      femaleCount++;      if (femaleCount >= targetSize) {        femaleCount = 0;        roomNum++;      }    });    setPassengerAllocations(newAllocs);    toast.success(`Allocated matching rules: ${sharingPref}-sharing rooms, same-gender groups locked.`);  };  const handleHotelEditClick = (idx: number) => {    setEditingHotelIdx(idx);    const hotel = hotelsList[idx] || {};    setEditingHotelData({      hotel: hotel.hotel || "",      dest: hotel.dest || "",      type: hotel.type || "",      plan: hotel.plan || "MAP (Breakfast + Dinner)",      roomsCount: hotel.roomsCount || 5,      guestsCount: hotel.guestsCount || 10,      nights: hotel.nights || 1,      status: hotel.status || "PENDING",      voucher: hotel.voucher || "",      amt: hotel.amt || 0,      paidAmt: hotel.paidAmt || 0    });    setHotelModalOpen(true);  };  const handleSaveHotelEdit = (e: React.FormEvent) => {    e.preventDefault();    if (editingHotelIdx === null) return;    const updated = [...hotelsList];    updated[editingHotelIdx] = {      ...updated[editingHotelIdx],      ...editingHotelData,      rooms: `${editingHotelData.roomsCount} Rooms`,      roomSub: `${editingHotelData.guestsCount} Guests`    };    setHotelsList(updated);    setHotelModalOpen(false);    toast.success(`Updated stay details for Day ${editingHotelIdx + 1}`);  };  const [roadVehiclesList, setRoadVehiclesList] = useState<any[]>([]);  const [editingTransportIdx, setEditingTransportIdx] = useState<number | null>(null);  const [transportModalOpen, setTransportModalOpen] = useState(false);  const [editingTransportData, setEditingTransportData] = useState<any>({    type: "",    cap: "",    capacityNum: 26,    seatsBooked: 26,    plate: "",    model: "",    total: "",    paid: ""  });  const handleTransportEditClick = (idx: number) => {    setEditingTransportIdx(idx);    const item = roadVehiclesList[idx] || {};    setEditingTransportData({      type: item.type || "Tempo Traveller",      cap: item.cap || "26 Seater",      capacityNum: item.capacityNum || 26,      seatsBooked: item.seatsBooked || 26,      plate: item.plate || "",      model: item.model || "",      total: item.total?.replace(/,/g, '') || "63000",      paid: item.paid?.replace(/,/g, '') || "0"    });    setTransportModalOpen(true);  };  const handleSaveTransportEdit = (e: React.FormEvent) => {    e.preventDefault();    if (editingTransportIdx === null) return;    const updated = [...roadVehiclesList];    const totalVal = parseInt(editingTransportData.total) || 0;    const paidVal = parseInt(editingTransportData.paid) || 0;    const dueVal = totalVal - paidVal;    updated[editingTransportIdx] = {      ...updated[editingTransportIdx],      ...editingTransportData,      seats: `${editingTransportData.seatsBooked} / ${editingTransportData.capacityNum}`,      total: totalVal.toLocaleString('en-IN'),      paid: paidVal.toLocaleString('en-IN'),      due: dueVal.toLocaleString('en-IN')    };    setRoadVehiclesList(updated);    setTransportModalOpen(false);    toast.success(`Updated transport details successfully`);  };  useEffect(() => {    const transAssignments = tripVendors.filter((v: any) => {      const vendorObj = typeof v.vendorId === 'object' ? v.vendorId : null;      const type = vendorObj?.type || v.vendorType || '';      return type === 'transport';    });    if (transAssignments.length > 0) {      const list = transAssignments.map((v: any, idx: number) => {        const vendorObj = typeof v.vendorId === 'object' ? v.vendorId : { name: 'Assigned Transport' };        const dayNum = idx + 1;        const { wd, date } = getDayDateAndWd(departureDateStr, idx);        const dest = tripDetails?.location || "Manali";        return {          id: v.id,          type: "Tempo Traveller",          cap: v.capacity || "26 Seater",          plate: v.notes || "17 Seater Tempo = 63000 (15th till 23rd July 2026)",          model: v.model || "Force Traveller",          vendor: vendorObj.name,          phone: vendorObj.phone || "+91 98765 43210",          from: "Ahmedabad",          fromTime: `${date.split(" ")[0]} ${date.split(" ")[1]}, 06:00 AM`,          to: dest,          toTime: `${date.split(" ")[0]} ${date.split(" ")[1]}, 06:00 PM`,          days: `${date.split(" ")[0]} ${date.split(" ")[1]}`,          daysCount: "2 Days",          seats: `${v.seatsBooked || 26} / ${v.capacityNum || 26}`,          seatsBooked: v.seatsBooked || 26,          capacityNum: v.capacityNum || 26,          total: v.agreedCost?.toLocaleString('en-IN') || "63,000",          paid: v.paidAmount?.toLocaleString('en-IN') || "0",          due: ((v.agreedCost || 63000) - (v.paidAmount || 0)).toLocaleString('en-IN'),          status: v.paymentStatus?.toUpperCase() || 'CONFIRMED'        };      });      setRoadVehiclesList(list);    } else {      setRoadVehiclesList([        {          id: "road-mock-1",          type: "Tempo Traveller",          cap: "26 Seater",          plate: "17 Seater Tempo = 63000 (15th till 23rd July 2026)",          model: "Force Traveller",          vendor: "17 Seater Tempo",          phone: "+91 98765 43210",          from: "Ahmedabad",          fromTime: "14 Jul, 06:00 AM",          to: "Himachal Pradesh",          toTime: "14 Jul, 06:00 PM",          days: "14 Jul",          daysCount: "2 Days",          seats: "26 / 26",          seatsBooked: 26,          capacityNum: 26,          total: "63,000",          paid: "0",          due: "63,000",          status: "CONFIRMED"        }      ]);    }  }, [tripVendors, departureDateStr, tripDetails]);  const transportVehiclesLabel = useMemo(() => {    const count = tripVendors.filter(v => v.vendorType === 'transport').length;    return count > 0 ? `${count} Vehicles Assigned` : "Assign Transport";  }, [tripVendors]);  const dateAndDurationLabel = useMemo(() => {    try {      const startDate = new Date(departureDateStr);      const daysMatch = tripDetails?.duration?.match(/(\d+)\s*Day/i);      const numDays = daysMatch ? parseInt(daysMatch[1], 10) : 9;      const endDate = new Date(startDate.getTime() + (numDays - 1) * 24 * 60 * 60 * 1000);            const formatOptions = { day: '2-digit', month: 'short', year: 'numeric' } as const;      const startStr = startDate.toLocaleDateString('en-US', formatOptions);      const endStr = endDate.toLocaleDateString('en-US', formatOptions);      return `${startStr} – ${endStr} (${tripDetails?.duration || '9D / 8N'})`;    } catch {      return `05 Jul 2027 – 13 Jul 2027 (9D / 8N)`;    }  }, [departureDateStr, tripDetails]);  const hasDateMismatch = useMemo(() => {    try {      const depDate = new Date(departureDateStr);      const createdDate = new Date("2027-06-15");      return depDate.getTime() < createdDate.getTime();    } catch {      return false;    }  }, [departureDateStr]);  const timelineSteps = useMemo(() => {    const confirmedBookings = bookings.filter((b: any) => b.status !== "cancelled");    const sortedBookings = [...confirmedBookings].sort((a: any, b: any) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime());        const firstBookingDate = sortedBookings[0] ? new Date(sortedBookings[0].createdAt).toLocaleString('en-IN', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) : "TBD";    const bookingStartedStr = sortedBookings[0] ? new Date(new Date(sortedBookings[0].createdAt).getTime() - 2 * 60 * 60 * 1000).toLocaleString('en-IN', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) : "TBD";        const capacity = tripDetails?.maxGroupSize || 30;    const filledPercentage = capacity > 0 ? (stats.totalParticipants / capacity) * 100 : 0;    const seats50PercentStr = sortedBookings[Math.floor(sortedBookings.length / 2)] ? new Date(sortedBookings[Math.floor(sortedBookings.length / 2)].createdAt).toLocaleString('en-IN', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) : "TBD";    const hotels = tripVendors.filter(v => v.vendorType === 'hotel');    const allHotelsConfirmed = hotels.length > 0 && hotels.every(h => h.paymentStatus === 'paid');    const hotelsConfirmStr = allHotelsConfirmed ? "Confirmed" : "Pending Confirmation";    const depDate = new Date(departureDateStr);    const departureDayStr = depDate.toLocaleString('en-IN', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' });    return [      { title: "Booking Started", date: bookingStartedStr, user: "System", active: sortedBookings.length > 0 },      { title: "First Booking Received", date: firstBookingDate, user: sortedBookings[0]?.name || "System", active: sortedBookings.length > 0 },      { title: "50% Seats Filled", date: seats50PercentStr, user: "Sales Desk", active: filledPercentage >= 50 },      { title: "All Hotels Confirmed", date: hotelsConfirmStr, user: "Ops Desk", active: allHotelsConfirmed },      { title: "Balance Collection In Progress", date: stats.totalVendorPayables > 0 ? "In Progress" : "Completed", user: "Accounts Desk", current: true },      { title: "Departure Day", date: departureDayStr, pending: true },    ];  }, [bookings, tripVendors, tripDetails, departureDateStr, stats]);  const getDayDateAndWd = (startStr: string, offsetDays: number) => {    try {      const d = new Date(startStr);      d.setDate(d.getDate() + offsetDays);      const dayNames = ["SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"];      const monthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];            const wd = dayNames[d.getDay()];      const dateFormatted = `${String(d.getDate()).padStart(2, '0')} ${monthNames[d.getMonth()]} ${d.getFullYear()}`;      return { wd, date: dateFormatted };    } catch (err) {      return { wd: "SAT", date: "05 Jul 2027" };    }  };  const computedItinerary = useMemo(() => {    const baseItin = (tripDetails?.itinerary && tripDetails.itinerary.length > 0)       ? tripDetails.itinerary.map((it: any) => ({          day: `Day ${it.day}`,          plan: it.title || it.location,          sub: it.description || "",          stay: it.stay || "—",          stayType: it.stay && it.stay !== "—" ? "Standard Stay" : "",          stayBadge: it.stay && it.stay !== "—" ? "STANDARD" : "",          travel: "Local / Transfer",          travelSub: "Planned Transfer",          meals: it.meals || "—",          activities: Array.isArray(it.activities) ? it.activities.join(" ") : it.activities || "—",          status: "ON TIME"        }))      : (tripId.toLowerCase().includes("spt") ? MOCK_SPITI_ITINERARY : [          { day: "Day 1", plan: "Ahmedabad → Chandigarh", sub: "Overnight Journey by Volvo", stay: "—", stayType: "", travel: "Volvo Bus", travelSub: "Departure: 08:00 PM", meals: "—", activities: "—", status: "ON TIME" },          { day: "Day 2", plan: "Chandigarh → Manali", sub: "Enroute Sightseeing", stay: "Manali", stayType: "Hotel Mountain View", stayBadge: "DELUXE", travel: "280 KM", travelSub: "7-8 Hrs", meals: "Breakfast Dinner", activities: "Hadimba Temple Mall Road Visit", status: "ON TIME" },          { day: "Day 3", plan: "Manali Local Sightseeing", sub: "Solang Valley & Rohtang Pass (if open)", stay: "Manali", stayType: "Hotel Mountain View", stayBadge: "DELUXE", travel: "Local", travelSub: "70 KM", meals: "Breakfast Dinner", activities: "Solang Valley Rohtang Pass", status: "ON TIME" },          { day: "Day 4", plan: "Manali → Kasol", sub: "Scenic Drive", stay: "Kasol", stayType: "Riverside Camp", stayBadge: "CAMP", travel: "80 KM", travelSub: "3-4 Hrs", meals: "Breakfast Dinner", activities: "Parvati Valley Kasol Market", status: "ON TIME" },          { day: "Day 5", plan: "Kasol → Kullu → Manikaran", sub: "Hot Springs Visit", stay: "Kasol", stayType: "Riverside Camp", stayBadge: "CAMP", travel: "60 KM", travelSub: "2-3 Hrs", meals: "Breakfast Dinner", activities: "Manikaran Sahib Hot Springs", status: "ON TIME" },          { day: "Day 6", plan: "Kasol → Amritsar", sub: "Overnight Journey by Volvo", stay: "—", stayType: "", travel: "Volvo Bus", travelSub: "Departure: 08:00 PM", meals: "Breakfast", activities: "—", status: "ON TIME" },          { day: "Day 7", plan: "Amritsar Sightseeing", sub: "Heritage & Wagah Border", stay: "Amritsar", stayType: "Hotel Grand Amritsar", stayBadge: "DELUXE", travel: "Local", travelSub: "60 KM", meals: "Breakfast Dinner", activities: "Golden Temple Wagah Border", status: "ON TIME" },          { day: "Day 8", plan: "Amritsar → Delhi", sub: "Overnight Journey by Train", stay: "—", stayType: "", travel: "Shatabdi Express", travelSub: "Departure: 07:00 PM", meals: "Breakfast", activities: "—", status: "ON TIME" },          { day: "Day 9", plan: "Delhi → Ahmedabad", sub: "Arrival in Ahmedabad", stay: "—", stayType: "", travel: "Flight", travelSub: "Arrival: 06:30 AM", meals: "—", activities: "—", status: "ON TIME" },        ]);    return baseItin.map((item: any, idx: number) => {      const { wd, date } = getDayDateAndWd(departureDateStr, idx);      return {        ...item,        wd,        date      };    });  }, [tripDetails, departureDateStr, tripId]);  const computedActivities = useMemo(() => {    if (tripId.toLowerCase().includes("spt")) {      return MOCK_SPITI_ACTIVITIES;    }    return [      { day: "Day 1", wd: "05 Jul, Sat", act: "Volvo Journey", sub: "Ahmedabad → Chandigarh", type: "TRAVEL", inc: true, time: "09:00 PM", loc: "Ahmedabad", status: "CONFIRMED", statusClass: "bg-emerald-50 text-emerald-600 border-emerald-100" },      { day: "Day 2", wd: "06 Jul, Sun", act: "Manali Local Sightseeing", sub: "Hidimba Temple, Mall Road", type: "SIGHTSEEING", inc: true, time: "10:00 AM - 06:00 PM", loc: "Manali", status: "CONFIRMED", statusClass: "bg-emerald-50 text-emerald-600 border-emerald-100" },      { day: "Day 3", wd: "07 Jul, Mon", act: "Solang Valley Visit", sub: "Ropeway, Snow Point (if Open)", type: "SIGHTSEEING", inc: true, time: "09:30 AM - 05:00 PM", loc: "Solang Valley", status: "CONFIRMED", statusClass: "bg-emerald-50 text-emerald-600 border-emerald-100" },      { day: "Day 4", wd: "08 Jul, Tue", act: "Kasol Visit", sub: "Kasol Market, Cafes", type: "SIGHTSEEING", inc: true, time: "11:00 AM -